# ---------------------------------------------------------
# cd with fzf
# ---------------------------------------------------------
function zd() {
  local dir
  dir=$(fd --hidden --type directory . ${1:-.} | fzf +m)
  [ -n "$dir" ] && cd "$dir"
}

