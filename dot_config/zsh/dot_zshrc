# ---------------------------------------------------------
# Sheldon
# ---------------------------------------------------------
eval "$(sheldon source)"

# pnpm
export PNPM_HOME="/home/yuji/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end
